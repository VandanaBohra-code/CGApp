import { toggleIndicatorUrl } from '../../../teams/components/SplitButton/toggleIndicatorUrl';
var getIndicatorStyles = function getIndicatorStyles(color, outline, size) {
  return {
    content: '""',
    width: size,
    height: size,
    backgroundImage: toggleIndicatorUrl(color, outline),
    backgroundRepeat: 'no-repeat'
  };
};
export var splitButtonToggleStyles = {
  root: function root(_ref) {
    var p = _ref.props,
      v = _ref.variables,
      theme = _ref.theme;
    var siteVariables = theme.siteVariables;
    var borderWidth = siteVariables.borderWidth;
    return Object.assign({
      ':before': Object.assign({}, getIndicatorStyles(p.disabled ? 'GrayText' : 'ButtonText', true, v.toggleButtonIndicatorSize))
    }, p.primary && {
      borderWidth: borderWidth,
      borderColor: "transparent"
    }, p.disabled && {
      borderWidth: borderWidth,
      borderColor: "transparent"
    });
  }
};
//# sourceMappingURL=splitButtonToggleStyles.js.map
